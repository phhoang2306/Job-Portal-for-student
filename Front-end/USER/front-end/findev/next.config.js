/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['d22eih62xsx9m1.cloudfront.net', 'via.placeholder.com', 'firebasestorage.googleapis.com'],
  },
};

module.exports = nextConfig;
